const users = require("./Users");
const category = require("./Category");
const orders = require("./Orders");
const carts = require("./Carts");
const products = require("./Products");

module.exports = { users, category, carts, orders, products };
